import type { Meta } from '@storybook/react';

import BaseCase from './BaseCase.tsx';

const meta: Meta<typeof BaseCase> = {
  component: BaseCase,
  title: 'Basic use-case',
};

export default meta;

export const Primary = {};
